# Domain docs

本仓库采用单上下文布局。

工程类 skill 在探索代码前应读取：

- 根目录的 `CONTEXT.md`（如存在）
- `docs/adr/` 中与当前工作有关的架构决策（如存在）

输出中的领域概念应沿用 `CONTEXT.md` 定义的术语。若方案与现有 ADR 冲突，必须明确指出，不得静默覆盖。缺少这些文件时继续工作，无需预先创建；待领域术语或架构决策真正形成后再补充。
