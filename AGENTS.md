# AGENTS.md — 个人博客

## Agent skills

### Issue tracker

PRD 与开发任务统一记录在 `bilbillm/bilbillm.github.io` 的 GitHub Issues。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个标准分诊标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

本项目采用单上下文布局：根目录使用 `CONTEXT.md`，架构决策记录在 `docs/adr/`。详见 `docs/agents/domain.md`。
