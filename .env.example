# Optional Vercount-compatible event endpoint. Leave blank to render no public counter.
# If omitted entirely, the public Vercount endpoint is used. PUBLIC_VERCOUT_ENDPOINT remains supported for legacy setups.
PUBLIC_VERCOUNT_ENDPOINT=
